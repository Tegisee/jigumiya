# 010. 상품 정보 스크래핑 + 쿠팡 파트너스

## 상태: ✅ 스크래핑 구현 완료 / ⏸ 파트너스 API 보류 (15만원 달성 후)

## 상품 정보 스크래핑 (WebView 방식)

### 배경: 서버사이드 스크래핑 불가
- 쿠팡은 모든 서버 User-Agent를 403으로 차단 (curl, fetch, 모든 UA)
- React Native에서 fetch()해도 200이지만 2KB 봇 차단 페이지 반환
- Google 캐시, Naver 검색 등 우회도 불가
- **해결: Hidden WebView에서 실제 브라우저 엔진으로 DOM 스크래핑**

### 구현 방식
1. `CoupangScraper.tsx` — 숨겨진 WebView (0x0 크기, opacity 0)
2. 쿠팡 URL 로드 → link.coupang.com 단축 URL도 자동 리다이렉트
3. `onNavigationStateChange`로 상품 페이지(`/vp/products/`, `/vm/products/`) 도착 감지
4. `onLoadEnd` 이벤트로 로딩 완료 감지 (수동 URL 입력 대응)
5. 2초 대기 후 `injectJavaScript(SCRAPE_JS)` 실행
6. DOM에서 추출: 상품명(OG → h2 → title), 가격(DOM → OG → JSON-LD), 이미지(OG → img)
7. `postMessage`로 React Native에 결과 전달
8. `price > 0` 체크 — 중간 페이지(리다이렉트)는 무시, price=0이면 재시도 허용

### 딥링크 차단
- `injectedJavaScriptBeforeContentLoaded`로 `window.location` setter override
- `coupang://`, `itms-appss://`, `intent://` 스킴 할당을 무시
- `app.json`에 `LSApplicationQueriesSchemes: ["coupang", "itms-appss"]` 추가

### 상품명 정리
- OG 타이틀에서 `|` 또는 `-` 이후 전체 제거: `title.replace(/\s*[|\-].*$/, '').trim()`
- 프로토콜 없는 이미지 URL 보정: `//xxx` → `https://xxx`

### URL 파싱 (수동 입력)
- 붙여넣기 시 "상품명\nURL" 형태 대응
- `extractUrl()` — 텍스트에서 `https://` URL만 정규식 추출

### 핵심 파일
| 파일 | 역할 |
|------|------|
| components/CoupangScraper.tsx | Hidden WebView + DOM 스크래핑 |
| app/modal/add-item.tsx | Share Intent / 수동 입력 → 스크래핑 → 저장 |
| services/productMeta.ts | 공유 텍스트 파싱 (fallback용, 레거시) |

### 실기기 테스트 결과
- ✅ Share Intent로 쿠팡 링크 공유 → 상품명/가격/이미지 정상 추출
- ✅ 플러스 버튼 URL 직접 입력 → 스크래핑 정상 동작
- ✅ link.coupang.com 단축 URL → www.coupang.com 리다이렉트 후 스크래핑 성공
- ✅ 이미지 렌더링 (목록 + 상세 화면)

---

## 쿠팡 파트너스 API (보류)

### 활성화 조건
1. 수동 affiliate 링크로 15만원 누적 수익 달성
2. partners.coupang.com에서 최종승인 신청
3. 승인 완료 후 API 키(ACCESS_KEY / SECRET_KEY) 발급
4. .env에 설정 → `hasCoupangApiKeys()` true → 자동 딥링크 변환 활성화

### 코드 준비 완료
- services/coupangApi.ts — HMAC 서명 + POST 딥링크 생성 API
- services/config.ts — EXPO_PUBLIC_COUPANG_ACCESS_KEY / SECRET_KEY 초기화
- add-item.tsx — `hasCoupangApiKeys()` 체크 → API 키 있으면 자동 변환

### 출시 전 필수: 파트너스 안내 문구
앱 내 어딘가에 아래 문구 삽입 필요 (앱스토어 심사 + 파트너스 정책):
> "이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다."
- 위치: 설정 화면 하단 또는 상세 화면 구매 버튼 근처
- 출시 직전에 추가 예정

---

## 쿠팡 파트너스 API Rate Limit (2026-04-19 조사)

> 파트너스 API 공식 한도 + 실측 기반 정리. 현재 활성 엔드포인트만 우선 기록.

### 엔드포인트별 한도
| 엔드포인트 | 한도 | 비고 |
|-----------|------|------|
| `/products/search` (상품 검색) | **분당 50회** | 가격 체크 봇 주 사용처 — 서버가 429 반환 시 백오프 필요 |
| `/deeplink` (딥링크 생성) | 공식 분당 한도 명시 없음 | 다만 429 응답 존재 확인 — 폭주 금지, 앱 내 상품 추가 시에만 사용 |
| `/reports` (실적 리포트) | **1시간 500회** | 현재 미사용 |
| `/products/reco` v2 (개인화 추천) | **분당 100회** | 현재 미사용, ADID 기반 개인화 — Phase 3-C 이후 검토 대상 |

### 현재 사용 현황 (GitHub Actions 가격 체크 봇)
- 호출 엔드포인트: `/products/search` (상품 키워드 검색으로 가격 매칭)
- 실행 패턴: **완전 순차 for-of + 상품당 1초 `setTimeout` 딜레이** (`scripts/price-checker/index.ts` L204)
- 실측 호출 빈도: **분당 약 35회** (상품당 ~1.7초: API 왕복 + 1초 sleep + Firestore update)
- 분당 50회 한도 대비 **30% 여유** 확보 — 안전 범위
- 실패 시 `continue`로 스킵, 딜레이도 생략 — 오히려 API 부하 감소

### ⚠️ 형제 앱 (아이고) 동시 실행 리스크
- 지금이야 + 아이고가 **동일 API 키** 공유 (이전 세팅 기준)
- 동시 시간대(예: 08:00 KST) 실행 시 **분당 요청이 합산**되어 50회 초과 가능
- 조치 필요: 아이고 `price-checker` cron 스케줄을 **지금이야와 1시간 이상 어긋나게 조정**
  - 예: 지금이야 08:00 / 14:00 / 21:00 → 아이고 09:00 / 15:00 / 22:00
- 장기적으로는 API 키 분리 검토 (파트너스 계정 2개 운영)

### reco v2 API 검토 (Phase 3-C 이후)
- ADID(광고 식별자) 기반 개인화 상품 추천
- 분당 100회 한도로 `/products/search`의 2배 여유
- 활용 가능성:
  - 홈 "오늘의 추천" 섹션 (현재 골드박스 대체 또는 병행)
  - 자주사는상품 기반 유사 상품 추천
- 제약: iOS는 `AppTrackingTransparency` 권한 필요 (ATT 허용률 현실적으로 30~40%)
- 우선순위: Phase 3-D MVP 안정화 + Phase 3-C 서버 이관 완료 후 재검토

### 향후 최적화 아이디어 (알림 로직)
- 현재: 하루 3회 가격 체크 + 3회 모두 알림 발송 (무변동 포함)
- 개선안: **1~2회차는 가격 DB 갱신만, 마지막(21:00) 회차에만 알림 발송**
- 효과: 유저 체감 알림 피로 감소, 1~2회차는 API 호출 + DB 쓰기만 발생
- 구현 위치: `scripts/price-checker/index.ts` 알림 조건 판별부에 시간대 가드 추가
- 검토 시점: 파트너스 실적 검증 후 (2026-04-20+)

### `/products/search` 호출량 근본 최적화 (Phase 3-B/3-C 연계)
- 현재: 유저별 개별 상품 문서를 전부 순회 → 같은 productId도 중복 호출
- 근본 해결: **`shared_products` 단일 레코드로 통합 후 고유 상품 수만큼만 호출**
  - Phase 3-A 이중 쓰기 완료 (2026-04-18) → Phase 3-B 백필 → Phase 3-C 서버 이관 시 적용
  - 예상 효과: 중복 productId 제거 → 호출량 30~50% 감소 예상 (유저 증가에도 선형 증가 완화)
- 중간 조치 (당장 필요): **아이고 cron 스케줄을 지금이야(08/14/21 KST)와 1시간 이상 어긋나게 조정**

---

## 파트너스 실적 미집계 원인 확정 및 조치 (2026-04-20)

### 근본 원인
공유 단축 URL `link.coupang.com/a/...`가 **iOS Universal Link로 선제 흡수**되면서 `add-item.tsx:125` `fetch(parsedUrl, { redirect: 'manual' })`가 `Location` 헤더를 얻지 못함
→ `resolved`가 `link.coupang.com/a/...` 그대로 유지
→ `resolved.includes('/vp/') || resolved.includes('/vm/')` 조건 불통과
→ **`generateDeepLink` 미호출 → 원본 공유 URL 그대로 저장 → 수수료 트래킹 끊김**

### 임시 수정 (커밋 `673c601`, 1.0.4 bn37/vc37)
- `add-item.tsx` 딥링크 생성 조건을 `resolved.includes('coupang.com')`로 완화
- `handleNext`(L142) + `handleSave`(L247) 두 지점 모두 적용
- **한계**: `link.coupang.com/a/...`는 이미 단축된 파트너스 링크 → `/deeplink` API가 원본 URL(`www.coupang.com/vp/...`)을 기대하므로 `rCode !== '0'`로 실패 가능성 높음
- 실패해도 폴백(원본 URL 유지)은 유지되므로 앱 동작에는 무해. **로그 `[CoupangAPI] 딥링크 응답: <rCode>`로 실제 성공률 검증 필요**
- 검증 지표: `link.coupang.com/a/...` 입력 시 `shortenUrl`이 생성되면 성공, `null`이면 실패

### 근본 해결책 (향후 구현)
- **Firebase Functions에서 서버사이드 resolve** 수행
  - 입력: 공유 URL (`link.coupang.com/a/...`)
  - 처리: 서버 측 `fetch`로 302 리다이렉트 추적 → `www.coupang.com/vp/products/...` 획득 → `/deeplink` API 호출 → `shortenUrl` 반환
  - 출력: 정상 파트너스 단축 링크
- iOS Universal Link는 **클라이언트에서만 발동** → 서버 fetch는 영향받지 않음
- 구현 위치 제안: `functions/resolveDeeplink` (Phase 3-C 서버 이관 시 함께)
- 클라이언트 변경: `add-item.tsx`에서 `generateDeepLink` 직접 호출 대신 Functions 엔드포인트 호출

### 검증 계획
- **2026-04-21 15:00 KST 이후** 파트너스 대시보드에서 실적 집계 여부 확인
- 테스트 방식: 가족 계정(다른 결제수단/배송지)으로 공유 링크 경유 구매
- 확인 포인트:
  1. 1.0.4 bn37/vc37(`673c601` 반영) 버전으로 공유 시 딥링크 API `rCode` 로그
  2. 실구매 발생 시 파트너스 대시보드 실적 반영 여부
  3. 집계되면: 임시 수정이 유효 → 근본 해결은 우선순위 낮춤
  4. 집계 안 되면: Firebase Functions 서버사이드 resolve 착수 (Phase 3-C와 병합 검토)
